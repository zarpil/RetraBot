{
  "name": "retrabot",
  "version": "1.0.2",
  "private": true,
  "workspaces": [
    "shared",
    "bot",
    "dashboard"
  ],
  "scripts": {
    "bot:dev": "npm run dev -w bot",
    "dashboard:dev": "npm run dev -w dashboard",
    "dev": "concurrently \"npm:bot:dev\" \"npm:dashboard:dev\""
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
